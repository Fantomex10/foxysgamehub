// firestore.rules (TEMP)
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMP: everything open
    match /{document=**} { allow read, write: if true; }
  }
}

